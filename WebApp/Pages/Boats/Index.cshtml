@page
@model WebApp.Pages_Boats.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Boat[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Boat[0].Size)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Boat[0].Amount)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Boat[0].GameOption)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Boat) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Size)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Amount)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GameOption.GameOptionId)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.BoatId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.BoatId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.BoatId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
