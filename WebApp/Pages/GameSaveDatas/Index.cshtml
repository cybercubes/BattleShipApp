@page
@model WebApp.Pages_GameSaveDatas.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GameSaveData[0].TimeStamp)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GameSaveData[0].SerializedGameData)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GameSaveData[0].SaveName)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.GameSaveData) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TimeStamp)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SerializedGameData)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SaveName)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.GameSaveDataId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.GameSaveDataId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.GameSaveDataId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
